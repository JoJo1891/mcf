{% extends 'base.html.twig' %}

{% block title %}Resos{% endblock %}

{% block body %}
    <h1>Resos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reso.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reso.name }}</td>
            </tr>
            <tr>
                <th>Liens</th>
                <td>{{ reso.liens }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resos_index') }}">back to list</a>

    <a href="{{ path('resos_edit', {'id': reso.id}) }}">edit</a>

    {{ include('resos/_delete_form.html.twig') }}
{% endblock %}
