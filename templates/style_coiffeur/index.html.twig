{% extends 'base.html.twig' %}

{% block title %}StyleCoiffeur index{% endblock %}

{% block body %}
    <h1>StyleCoiffeur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>IdCoiffeur</th>
                <th>IdStyle</th>
                <th>Etat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for style_coiffeur in style_coiffeurs %}
            <tr>
                <td>{{ style_coiffeur.id }}</td>
                <td>{{ style_coiffeur.idCoiffeur }}</td>
                <td>{{ style_coiffeur.idStyle }}</td>
                <td>{{ style_coiffeur.etat }}</td>
                <td>
                    <a href="{{ path('style_coiffeur_show', {'id': style_coiffeur.id}) }}">show</a>
                    <a href="{{ path('style_coiffeur_edit', {'id': style_coiffeur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('style_coiffeur_new') }}">Create new</a>
{% endblock %}
