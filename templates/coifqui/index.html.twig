{% extends 'base.html.twig' %}

{% block title %}Coifqui index{% endblock %}

{% block body %}
    <h1>Coifqui index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for coifqui in coifquis %}
            <tr>
                <td>{{ coifqui.id }}</td>
                <td>
                    <a href="{{ path('coifqui_show', {'id': coifqui.id}) }}">show</a>
                    <a href="{{ path('coifqui_edit', {'id': coifqui.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('coifqui_new') }}">Create new</a>
{% endblock %}
