<form method="post" action="{{ path('stylecoif_delete', {'id': stylecoif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stylecoif.id) }}">
    <button class="btn">Delete</button>
</form>
