{% extends 'base.html.twig' %}

{% block title %}Stylecoif{% endblock %}

{% block body %}
    <h1>Stylecoif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stylecoif.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stylecoif.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stylecoif_index') }}">back to list</a>

    <a href="{{ path('stylecoif_edit', {'id': stylecoif.id}) }}">edit</a>

    {{ include('stylecoif/_delete_form.html.twig') }}
{% endblock %}
