{% extends 'base.html.twig' %}

{% block title %}Stylecoif index{% endblock %}

{% block body %}
    <h1>Stylecoif index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stylecoif in stylecoifs %}
            <tr>
                <td>{{ stylecoif.id }}</td>
                <td>{{ stylecoif.name }}</td>
                <td>
                    <a href="{{ path('stylecoif_show', {'id': stylecoif.id}) }}">show</a>
                    <a href="{{ path('stylecoif_edit', {'id': stylecoif.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('stylecoif_new') }}">Create new</a>
{% endblock %}
